MODEL: 
SETS:
MOIS:espace;
PERIODES:cout;
ARC(MOIS,PERIODES):X;
ENDSETS

DATA:
espace,cout=@OLE('Mercantile.xlsx');
ENDDATA

!fonction objectif;
MIN=@SUM(ARC:cout*X);

!contraintes;
@FOR(MOIS(m): @SUM(MOIS(k) | k#LE#m:@SUM(PERIODES(j) | k+j #GT#m:X(k,j)))>=espace(m));

END